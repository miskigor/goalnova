/** Supabase Storage bucket for user-uploaded / merged video objects. */
export const VIDEO_STORAGE_BUCKET = "pitchrusch-videos";
/** Legacy bucket name kept for backwards compatibility on older projects. */
export const LEGACY_VIDEO_STORAGE_BUCKET = "goalnova-videos";

/** Supabase Storage bucket for catalog MP3s (e.g. Pixabay sync script). */
export const MUSIC_STORAGE_BUCKET = "pitchrusch-music";
