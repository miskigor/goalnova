"use client";

/**
 * Retired — primary navigation is sidebar (desktop) + bottom tab bar (mobile).
 * Kept as a no-op so existing layout shells do not need structural changes.
 */
export function AppPrimaryTopNavStrip() {
  return null;
}
